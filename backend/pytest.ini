[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    needs_keys: requires vendor API credentials; skipped by default
    slow: excluded from the fast inner loop
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
